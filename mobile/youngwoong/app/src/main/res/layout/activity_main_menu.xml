<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- 배경 이미지 -->
    <ImageView
        android:id="@+id/background_main"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitXY"
        android:src="@drawable/main"
        android:contentDescription="배경" /> <!-- ✅ 직접 문자열로 지정하거나 strings.xml로 연결 -->

    <!-- 접수 버튼 (더 크게, 좌우 넓게 조정됨) -->
    <ImageView
        android:id="@+id/button_check_in"
        android:layout_width="match_parent"
        android:layout_height="425dp"
        android:layout_gravity="top"
        android:layout_marginStart="60dp"
        android:layout_marginTop="140dp"
        android:layout_marginEnd="60dp"
        android:contentDescription="접수 버튼"
        android:src="@drawable/check_in" /> <!-- ✅ 문자열 연결 또는 직접 작성 -->

    <!-- 길안내 버튼 (건드리지 않음) -->
    <ImageView
        android:id="@+id/button_guidance"
        android:layout_width="match_parent"
        android:layout_height="180dp"
        android:layout_gravity="top"
        android:layout_marginStart="100dp"
        android:layout_marginTop="580dp"
        android:layout_marginEnd="100dp"
        android:src="@drawable/guidance"
        android:contentDescription="길안내 버튼" /> <!-- ✅ 직접 문자열 작성 -->

</FrameLayout>
