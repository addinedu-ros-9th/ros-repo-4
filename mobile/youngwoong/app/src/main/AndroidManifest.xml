<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WaitingScreen">

        <activity android:name="com.example.youngwoong.MainMenuActivity" />

        <activity android:name="com.example.youngwoong.AuthenticationActivity" />

        <activity android:name=".GuidanceActivity" />


        <activity
            android:name="com.example.youngwoong.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.WaitingScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


    </application>

</manifest>
